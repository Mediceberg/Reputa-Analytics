# Development Environment Variables
# MongoDB Atlas (replace with your real credentials)
MONGODB_URI=mongodb+srv://placeholder:placeholder@cluster.mongodb.net/reputa-v3?retryWrites=true&w=majority
MONGODB_DB_NAME=reputa-v3

# Upstash Redis (replace with your real credentials)
UPSTASH_REDIS_REST_URL=https://placeholder.upstash.io
UPSTASH_REDIS_REST_TOKEN=placeholder-token

# Server Configuration
PORT=3001
NODE_ENV=development

# Admin Portal Password
ADMIN_PASSWORD=admin123

# Protocol Version
PROTOCOL_VERSION=3.0
