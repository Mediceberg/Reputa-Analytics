# 🎛️ دليل مكونات Admin Dashboard

## 📦 المكونات وكيفية استخدامها

---

## 1️⃣ StatCard - بطاقات الإحصائيات

### الموقع
```typescript
src/components/admin/StatCard.tsx
```

### الاستخدام
```typescript
import { StatCard } from '@/components/admin/StatCard';
import { Users, TrendingUp } from 'lucide-react';

<StatCard
  title="إجمالي الرواد"
  value={2216}
  icon={Users}
  color="emerald"
  suffix="رائد"
  index={0}
/>

<StatCard
  title="الدفعات الناجحة"
  value={450}
  icon={TrendingUp}
  color="cyan"
  index={1}
/>
```

### Props

| Prop | النوع | الوصف | مثال |
|------|-------|-------|------|
| `title` | string | عنوان البطاقة | "إجمالي الرواد" |
| `value` | number \| string | القيمة المعروضة | 2216 |
| `icon` | LucideIcon | أيقونة من Lucide | Users |
| `color` | 'emerald' \| 'cyan' \| 'purple' \| 'blue' | اللون | "emerald" |
| `suffix` | string (optional) | نص إضافي | "رائد" |
| `index` | number | ترتيب التأثير الحركي | 0 |

### الألوان المتاحة

```
Emerald   → أخضر (bg-emerald-500/10)
Cyan      → أزرق (bg-cyan-500/10)
Purple    → بنفسجي (bg-purple-500/10)
Blue      → أزرق داكن (bg-blue-500/10)
```

---

## 2️⃣ UsersTable - جدول المستخدمين

### الموقع
```typescript
src/components/admin/UsersTable.tsx
```

### الاستخدام
```typescript
import { UsersTable } from '@/components/admin/UsersTable';

const [selectedUser, setSelectedUser] = useState(null);

<UsersTable
  users={users}
  onUserClick={(user) => setSelectedUser(user)}
/>
```

### Props

| Prop | النوع | الوصف |
|------|-------|-------|
| `users` | User[] | مصفوفة المستخدمين |
| `onUserClick` | (user: User) => void | دالة عند الضغط على صف |

### User Interface

```typescript
interface User {
  id: string;
  uid: string;
  username: string;
  reputation_score: number;
  wallet_address: string;
  vip_status: boolean;
  joined_date: string;
  app_score: number;
  email: string;
}
```

### الميزات

✅ **البحث الفوري** - يبحث عن username أو email أو uid  
✅ **الترتيب** - انقر على العنوان للترتيب تصاعدي/تنازلي  
✅ **التصفية** - 10 مستخدمين في الصفحة (قابل للتعديل)  
✅ **الوسوم** - ألوان مختلفة حسب السكور:
- **أخضر**: > 80 (عالي)
- **أزرق**: 40-80 (متوسط)
- **رمادي**: < 40 (منخفض)

---

## 3️⃣ UserModal - نافذة تفاصيل المستخدم

### الموقع
```typescript
src/components/admin/UserModal.tsx
```

### الاستخدام
```typescript
import { UserModal } from '@/components/admin/UserModal';

const [selectedUser, setSelectedUser] = useState<User | null>(null);
const [isOpen, setIsOpen] = useState(false);

<UserModal
  user={selectedUser}
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
/>
```

### Props

| Prop | النوع | الوصف |
|------|-------|-------|
| `user` | User \| null | بيانات المستخدم |
| `isOpen` | boolean | هل النافذة مفتوحة |
| `onClose` | () => void | دالة عند الإغلاق |

### المعلومات المعروضة

```
┌─────────────────────────────────────┐
│        تفاصيل المستخدم              │
├─────────────────────────────────────┤
│  نقاط السمعة: 95 ✨                 │
│  ─────────────────────────────────── │
│  اسم المستخدم: Ahmed                │
│  البريد: ahmed@example.com           │
│  UID: user_123... [نسخ]              │
│  ─────────────────────────────────── │
│  المحفظة: 0x123...456 [نسخ]          │
│  ─────────────────────────────────── │
│  سكور التطبيق: 30                    │
│  تاريخ الانضمام: 2025-01-01          │
│  ─────────────────────────────────── │
│                   [إغلاق]             │
└─────────────────────────────────────┘
```

### الميزات

✅ **زر النسخ** - لكل حقل (مع تغيير الأيقونة عند النسخ)  
✅ **التأثيرات** - دخول سلس للـ Modal  
✅ **الخلفية الخافتة** - Backdrop مع blur  
✅ **الأيقونات** - تغيير عند النسخ بنجاح  

---

## 4️⃣ ChartDistribution - الرسم البياني

### الموقع
```typescript
src/components/admin/ChartDistribution.tsx
```

### الاستخدام
```typescript
import { ChartDistribution } from '@/components/admin/ChartDistribution';

<ChartDistribution
  data={{
    high: 400,
    medium: 1200,
    low: 616
  }}
/>
```

### Props

| Prop | النوع | الوصف |
|------|-------|-------|
| `data` | { high, medium, low } | توزيع المستخدمين |

### data Interface

```typescript
{
  high: number;    // > 80
  medium: number;  // 40-80
  low: number;     // < 40
}
```

### الرسم البياني

```
عالي (>80)      ████░░░░░░ 18.0%
متوسط (40-80)   ██████████ 54.1%
منخفض (<40)     ████░░░░░░ 27.9%
```

### الميزات

✅ **Recharts Library** - رسوم بيانية احترافية  
✅ **Tooltip** - معلومات عند التمرير  
✅ **Colors** - متطابقة مع الوسوم  
✅ **Legend** - إحصائيات أسفل الرسم  

---

## 🎛️ Dashboard Page

### الموقع
```typescript
src/app/admin/dashboard/page.tsx
```

### التخطيط الكامل

```
┌─────────────────────────────────────────────┐
│    Header: لوحة التحكم الإدارية              │
├─────────────────────────────────────────────┤
│ [StatCard] [StatCard] [StatCard] [StatCard] │
├─────────────────────────────────────────────┤
│  [Chart] [Additional Stats]                 │
├─────────────────────────────────────────────┤
│  UsersTable (Search + Pagination + Sort)    │
├─────────────────────────────────────────────┤
│  Footer: آخر تحديث                          │
└─────────────────────────────────────────────┘
```

### الحالات

#### حالة التحميل
```typescript
<Loader /> // أيقونة تدور
```

#### حالة الخطأ
```typescript
<div className="text-red-400 text-lg font-bold">
  ❌ فشل في تحميل البيانات
  [إعادة محاولة]
</div>
```

#### حالة النجاح
```typescript
// عرض كل المكونات
```

---

## 📡 API Endpoint

### الموقع
```typescript
api/admin/dashboard/route.ts
```

### الطلب

```typescript
GET /api/admin/dashboard
```

### الاستجابة

```json
{
  "success": true,
  "stats": {
    "totalPioneers": 2216,
    "totalPayments": 450,
    "totalTransactions": 5850,
    "averageReputation": 65,
    "totalUsers": 2216
  },
  "scoreDistribution": {
    "high": 400,
    "medium": 1200,
    "low": 616
  },
  "users": [
    {
      "id": "...",
      "uid": "user_123",
      "username": "Ahmed",
      "reputation_score": 95,
      "wallet_address": "0x123...456",
      "vip_status": true,
      "joined_date": "2025-01-01T00:00:00Z",
      "app_score": 30,
      "email": "ahmed@example.com"
    }
  ]
}
```

### معدلات الخطأ

```
200 OK              ✅ البيانات جاهزة
500 Internal Error  ❌ خطأ في الخادم
```

---

## 🎨 نظام الألوان

### الثيم العام

```css
/* الخلفيات */
bg-slate-950      /* أسود عميق */
bg-slate-900      /* أسود */
bg-slate-800      /* أسود فاتح */

/* النصوص */
text-emerald-300  /* أخضر فاتح */
text-cyan-400     /* أزرق نيون */
text-gray-400     /* رمادي متوسط */

/* الحدود */
border-emerald-500/20  /* أخضر شفاف */
border-cyan-500/20     /* أزرق شفاف */
```

### وسوم السكور

```
عالي (>80):    Emerald   - أخضر
متوسط (40-80): Cyan      - أزرق
منخفض (<40):   Gray      - رمادي
```

---

## 🔄 تدفق البيانات

```
┌──────────────┐
│   Dashboard  │ (صفحة React)
└──────┬───────┘
       │ useEffect
       │ fetch()
       ▼
┌──────────────┐
│ API Route    │ (/api/admin/dashboard)
└──────┬───────┘
       │ connectDB()
       │ MongoDB Queries
       ▼
┌──────────────┐
│  MongoDB     │
│ - final_users_v3
│ - global_stats
└──────────────┘
```

---

## 🎯 أمثلة الاستخدام المتقدم

### إضافة Filter مخصص

```typescript
const filteredUsers = users.filter(u => 
  u.reputation_score > 80 && u.vip_status === true
);
```

### تصدير البيانات

```typescript
const exportToCSV = () => {
  const csv = [
    ['username', 'reputation_score', 'email'],
    ...users.map(u => [u.username, u.reputation_score, u.email])
  ];
  // إنشاء ملف CSV...
};
```

### تطبيق Sorting مخصص

```typescript
const sortByScore = () => {
  setUsers(prev => 
    [...prev].sort((a, b) => b.reputation_score - a.reputation_score)
  );
};
```

---

## 📚 المراجع

- [Lucide Icons](https://lucide.dev)
- [Framer Motion](https://www.framer.com/motion/)
- [Recharts](https://recharts.org/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Next.js 14](https://nextjs.org/)

---

**✨ مكونات متطورة وسهلة الاستخدام!**
